# Unified AIOS + Hermes runtime requirements.
#
# Target:
#   Python 3.11
#   one environment for:
#     - demo/aios_demo.py
#     - Camel workforce agents
#     - Hermes SDK client side
#     - Hermes ACP subprocess
#
# Install into a fresh env. Do not install this into the existing Python 3.10
# conda env named "aios".

# Local projects
-e ./camel-master
-e ${AIOS_HERMES_AGENT_ROOT}[acp,web]

# Shared core pins resolved in favor of Hermes, because Hermes requires
# Python >=3.11 and OpenAI 2.x.
openai>=2.21.0,<3
anthropic>=0.39.0,<1
agent-client-protocol>=0.9.0,<1.0
mcp>=1.16.0,<2
pydantic>=2.12.5,<3
pydantic-settings>=2.11.0,<3
python-dotenv>=1.2.1,<2
httpx[socks]>=0.28.1,<1
requests>=2.33.0,<3
PyYAML>=6.0.2,<7
Jinja2>=3.1.6,<4
rich>=14.3.3,<15
tenacity>=9.1.4,<10
prompt_toolkit>=3.0.52,<4
PyJWT[crypto]>=2.12.0,<3

# Hermes / tool integrations used by the Hermes agent.
exa-py>=2.9.0,<3
firecrawl-py>=4.16.0,<5
parallel-web>=0.4.2,<1
fal-client>=0.13.1,<1
edge-tts>=7.2.7,<8

# AIOS app / UI / service layer.
Flask==2.3.3
flask-cors>=6.0.2,<7
flask-socketio==5.3.4
python-socketio==5.9.0
python-engineio==4.7.1
streamlit==1.51.0
sse-starlette==3.0.2
uvicorn[standard]>=0.37.0,<1
python-multipart==0.0.20

# AIOS data / document / media tools.
numpy==2.2.6
pandas==2.3.3
opencv-python==4.12.0.88
pillow==10.4.0
pyarrow==21.0.0
python-docx==1.2.0
qdrant-client==1.15.1
scikit-learn==1.7.2
scipy==1.15.3
yt-dlp==2025.9.26
ffmpeg==1.4

# ML stack used by existing AIOS agents/tools.
torch==2.9.0
transformers==4.57.1
sentence-transformers==5.1.2
safetensors==0.6.2
tiktoken==0.7.0
tokenizers==0.22.1
huggingface-hub==0.36.0

# Existing AIOS utility/dev dependencies.
altair==5.5.0
astor==0.8.1
GitPython==3.1.45
greenlet==3.2.4
grpcio==1.76.0
loguru==0.7.3
lxml==6.0.2
mypy==1.18.2
pathspec==0.12.1
playwright==1.55.0
portalocker==3.2.0
protobuf==6.33.0
psutil==5.9.8
pygame==2.6.1
pytest==8.4.2
toml==0.10.2
tomli==2.3.0
watchdog==6.0.0
websockets>=15.0.1,<16

# Windows-only package from the old requirements.txt. Kept guarded so Linux
# installs do not fail.
pywin32==311; sys_platform == "win32"
